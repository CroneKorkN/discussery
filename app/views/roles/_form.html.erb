<%= form_for(@role_type) do |f| %>
  <% if @role_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@role_type.errors.count, "error") %> prohibited this role_type from being saved:</h2>

      <ul>
      <% @role_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  
  <% PermissionType.all.each do |permission_type| %>
      <%= check_box_tag :permission_type_ids, permission_type.id, @role_type.permission_types.include?(permission_type), name: 'role_type[permission_type_ids][]' %>
      <%= label_tag :permission_type_ids, "#{permission_type.action}" %><br>
  <% end %>
  
  <div class="actions">
    <br><%= f.submit %>
  </div>
<% end %>
